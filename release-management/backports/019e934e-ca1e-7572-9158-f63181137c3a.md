---
id: 019e934e-ca1e-7572-9158-f63181137c3a
title: "backport: remove ambiguous CephContext fwd decl in rgw_keystone_scope.h"
component: rgw
category: integration

provenance:
  type: other
  other: |
    Downstream-only build adaptation. The Keystone scope backport (UUID
    019e92ed-d2cc-7bce-a481-77ca769cfda9, merge c582732911a7) carries a
    redundant forward declaration of `CephContext` that becomes
    ambiguous against the existing declaration on the 19.2.3 base.
    Removes the duplicate so the file compiles. No upstream PR.

justification:
  type: engineering

risk:
  blast: cosmetic
  conflict: clean
  coverage: strong
---

## Justification (public)

Build adaptation: removes a redundant `class CephContext;` forward
declaration from `src/rgw/rgw_keystone_scope.h` that becomes ambiguous
against the existing declaration on the 19.2.3 base.

## Justification (internal)

Required to build the Keystone scope + role-injection backport against
19.2.3. The upstream version of the header carries the forward
declaration; on 19.2.3 the symbol is already in scope at the point of
inclusion, and the duplicate triggers a compile error.

## Risk notes

Removes 2 lines from a header. No runtime behavior. Compiles cleanly
in CI.

## Behavior change

None.

## Upgrade notes

None.
