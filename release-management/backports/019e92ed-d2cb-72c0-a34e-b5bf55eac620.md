---
id: 019e92ed-d2cb-72c0-a34e-b5bf55eac620
title: "Write Lock - lua: postAuth hook, bucket tags, request blocking"
component: rgw
category: feature

provenance:
  type: upstream_pr
  upstream_pr_state: open-in-review
  upstream_prs:
  - https://github.com/ceph/ceph/pull/66065
  - https://github.com/ceph/ceph/pull/67219
  - https://github.com/ceph/ceph/pull/66567
  - https://github.com/ceph/ceph/pull/67548
  upstream_commits:
  - 68396f6337b5bb91b7e31593754d0dc95b5e9135
  - 6e09062fcc4a623ab8ac9f1f0e8ec78456f7fe28
  - d6bc0c5be8625bd7c5abfcd66700019616423583
  - fba30067e93cbb538a30cb5c2dfde470c253f567

justification:
  type: customer
  refs:
  - https://github.com/cobaltcore-dev/cloud-storage/issues/310

risk:
  blast: availability
  conflict: trivial
  coverage: partial
---

## Justification (public)

Lua script extensions for RGW to allow "Write Lock" feature

- New `postAuth` Lua hook point that runs after authentication /
  authorization but before the operation executes.
- Lua scripts can now read bucket tags.
- Lua scripts can abort request processing.

## Justification (internal)

Create an alternative to custom Swift middleware that allows write locking
buckets based on `X-Container-Meta-Write-Restricted: true` headers.

## Risk notes

The `postAuth` hook runs on every request. A slow or buggy Lua script
will add latency to all S3 operations.

## Behavior change

A new `postAuth` Lua hook point is available. Scripts can read bucket
tags and abort request processing. Lua is **inactive** out of the box
(no scripts are uploaded by default).

## Upgrade notes

No dedicated feature flag; controlled by whether a Lua script is uploaded
via `radosgw-admin`.
